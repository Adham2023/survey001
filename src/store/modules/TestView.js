const state = {
    currentAnswer: [],
    currentQuestion: []
}

const getters = {

}

const mutations = {
    SET_ANSWER(state, answer) {
       
    }
}

const actions = {
    SET_ANSWER(context, answer) {
       
    }
}

export const TestView = {
    state,
    getters,
    mutations,
    actions
}